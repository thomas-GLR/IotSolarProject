import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'iot-solar-web-app',
  webDir: 'dist/iot-solar-web-app/browser'
};

export default config;
